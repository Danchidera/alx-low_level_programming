#include "main.h"
/**
 * _absolut value: computes the absolute value of a
 * integer.
 *
 * Return: absolute value
 */
int _abs(int)
{
	if (num >= 0)
	{
		return (num);
	}
	else
	{
		return (num * -1);
	}
}
